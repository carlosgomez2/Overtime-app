<div class="container-fluid">
  <% if @pending_audit_confirmations.present? %>
    <div class="pending-homepage">
      <h2 class="text-center">Pending Your Confirmation</h2>
      <hr />
      <div class="container">
        <%= render partial: 'pending_audit_confirmations', locals: { pending_audit_confirmations: @pending_audit_confirmations } %>
      </div>
    </div>
  <% end %>

  <div class="pending-homepage">
    <h2 class="text-center">Request Overtime</h2>
    <hr />
    <div class="container">
      <%= link_to "Request Overtime Approval", new_post_path, class: 'btn btn-primary btn-block btn-lg' %>
    </div>
  </div>
</div>

